// import GistGateway from './GistGateway'
//
// describe('When listing gists', () => {
//   describe('and there are no gists', () => {
//     it('returns none', async () => {
//       const gateway = new GistGateway({ token: 'auth-token-here' })
//       const gists = await gateway.all()
//
//       expect(gists).toEqual([])
//     })
//   })
//
//   describe('and there is a gist', () => {
//     it('returns the gist', async () => {
//       const gateway = new GistGateway({ token: 'auth-token-here' })
//       const gists = await gateway.all()
//
//       expect(gists).toEqual([{
//         lol: true
//       }])
//     })
//   })
// })
